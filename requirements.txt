# 问巴菲特与达利欧 | Ask Buffett & Dalio
# Core dependencies
streamlit>=1.35
yfinance>=0.2.50
pandas>=2.0
numpy>=1.24
openai>=1.0
python-dotenv>=1.0

# Required for st.dataframe background_gradient styling
matplotlib>=3.5
jinja2>=3.0

# Optional — bypasses Yahoo Finance rate limits (strongly recommended)
# Note: curl_cffi has native extensions; may not install on all platforms.
# If installation fails, yfinance automatically falls back to requests.
curl_cffi>=0.6; python_version < "3.14"
